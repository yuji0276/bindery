## Data model
- action どのような操作を行うか
- mode nvimのinsert, zellijのtabを入れる
- tool ツールの名前
- key 割り当てられたキーバインド

定義元ジャンプなどの機能を後につけるためcolumn number,filepathなどもあとで入れるが、ここは拡張可能。衝突検知のために必要な最低限から始める

### key列の値域(型)

key列は行の集合から一つの値を導く写像として捉えることができる。ここではkeyのとりうる値域(型)について記述する。考慮しないといけないのは以下である。

- 同時押し or 順次押し
- 修飾キーを複数持つかどうか

これらを考慮して値域の集合を考える。構成は3段になる。
M := 修飾キーの語彙(Ctrl,Alt,Cmdなど。なんと綴るかは未確定)
B := 基底キーの語彙(t,=,leftなど。なんと綴るかは未確定)

1. 修飾キーの組み合わせ(Mの冪集合)
2. 1打鍵(修飾キーと基底キーの組み合わせ。Mのべき集合とBの直積)
3. 一つの割り当て(1打鍵の空ではない有限列)

例）

|入力|値域の元として|
|---|---|
|`Ctrl+Shift+t`|長さ1の列 `< ({Ctrl,Shift},t) >`|
|`<leader>ff`|長さ3の列 `< (∅,leader),(∅,f),(∅,f) >`|
|wezterm `SUPER` + `=`|長さ1の列 `< ({Super},=) >`|
|zellij `Ctrl+t` -> `n`|長さ2の列 `< ({Ctrl},t),(∅,n) >`|

空列を許さない理由：長さ0で何も押さないことになるから。キーとして意味をなさない。

### 競合判定

競合するかどうかの条件は以下の２つ。2つの条件がどちらも真になる時衝突していると言える

- モードが共存するかどうか
- 一方のキーが他方のキーの接頭辞であるかどうか

競合条件そのものにtoolは現れない。ただし共存可能かどうかを判定する内部では、2つのモードが同じツールに属するかどうかと、ツール同士の入れ子順を使う。

#### モードが共存するかどうか

２つの割り当てのモードが共存可能かどうかで決まる。

次の表は後述するCの検算である。3行ともCから導けなければならない。

| 組| 競合するかどうか|共存可能か|
|--|--|--|
|(zellij tab, nvim normal)|競合しない|排他|
|(nvim normal,nvim insert)|競合しない|排他|
|(nvim normal,zellij normal)|競合する|**共存可能**|

##### bindery.confを用いたモードの共存判定

bindery.confには各ツールのモードの透過性、ツール同士の入れ子構造を記述する。
入れ子構造を設定ファイルに置くのは、これが固定値ではなく環境ごとの宣言だからである。zellijを使わずwezterm上で直接nvimを開く構成もあるし、ssh先で入れ子が変わる構成もある。

判定する関数をC(m1,m2)とおく。m1,m2は比較する2つの割り当てが持つモードである。

1. m1,m2が同じツール->排他
2. m1,m2が違うツール->入れ子構造をみて外側のツールの**モード**の透過性を見る。透過なら共存可能。透過でないなら排他。

#### 一方のキーが他方のキーの接頭辞であるかどうか

`<leader>ff`と`<leader>f`は衝突するためこの条件が必要。
完全一致は長さの等しい接頭辞であるため、この条件に含まれる。同じキーが2つのツールに割り当てられているケースもここで拾われる。

## Open Question
- action 列の値域（意味の正規化）
- 完全一致と接頭辞の深刻度差（timeoutlen）
- Shift+a と A
- DisableDefaultAssignment
- CSI u・Kitty protocol
- wezterm のkey_tables・leader
- 基底キーと修飾キーの語彙の選択方法。a or A Cmd or CMDなど
- 3層以上のとき、間に挟まる層のモードは判定に効かなくてよいか（weztermのモードとnvimのモードの対では、間にzellijがいる）
